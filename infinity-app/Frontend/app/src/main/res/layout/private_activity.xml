<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_background">

    <ImageButton
        android:id="@+id/home_button"
        android:layout_width="75dp"
        android:layout_height="70dp"
        android:text="@string/home_button"
        android:layout_margin="16dp"
        android:background="@drawable/home_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:id="@+id/private_list"
        android:layout_width="400dp"
        android:layout_height="600dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/home_button">
    </ListView>

    <Button
        android:id="@+id/new_private"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:background="@null"
        android:layout_gravity="bottom"
        android:text="@string/new_something"
        android:textColor="@color/dark_blue"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/new_private_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/plus_image"
        app:layout_constraintBottom_toTopOf="@+id/new_private"
        app:layout_constraintEnd_toEndOf="@+id/new_private"
        app:layout_constraintStart_toStartOf="@+id/new_private"
        app:layout_constraintTop_toTopOf="@+id/new_private"
        app:srcCompat="@drawable/plus_image" />

</androidx.constraintlayout.widget.ConstraintLayout>